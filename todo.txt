Remaining TODOs

Future Feature: Temporary Skeleton Helpers
Add support for up to two temporary reanimated skeleton helpers.

Implementation notes:
- Track skeleton helpers separately from normal crew, for example with `state.helpers`.
- Use names such as `Skeleton 1` and `Skeleton 2`.
- Let helpers be assigned to eligible tasks and count as participants for allowed multi-person actions.
- Keep their Labor cost at 0.
- Do not track Exhaustion for them.
- Do not create labor saves, rest prompts, or meal prompts for them.
- Track their current or most recent action for the player-facing crew panel.
- Visually distinguish them from normal crew on the player screen.
- Use a combined actor list for action assignment, but apply Labor and Exhaustion logic only to real crew.
